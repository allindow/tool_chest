<h1>Login</h1>
<!-- we are not storing data for an object so we use session and specify the post path -->
<%= form_for(:session, url: login_path) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :password %>
<%= f.text_field :password %>
<%= f.submit "Sign In" %>
<% end %>
